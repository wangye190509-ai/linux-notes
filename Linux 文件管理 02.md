# Linux 文件管理 02

## 文件时间

```shell
stat [选项]... 文件...
```

- Access：访问时间，也叫 atime
  - 当文件被访问的时候，这个时间就会发生改变
  - Linux文件运行的时候查看文件又频繁数量又大，如果每次 atime 发生变化的时候都记入硬盘，或造成很大的压力。必须满足其中一个条件：
    - 自上次 atime 修改后，已达到 86400 秒
    - 发生写操作时
- Modify：修改时间，也叫 mtime
  - 当文件内容发生变化的时候，这个时间就会发生改变
- Change：改变时间，也叫 ctime
  - 当文件状态被改变的时候，这个时间就会发生修改

## 文件类型

| 标识符 | 文件类型     | 核心特征                                         | 常见示例                                                     | 实用场景                              |                               |
| ------ | ------------ | ------------------------------------------------ | ------------------------------------------------------------ | ------------------------------------- | ----------------------------- |
| `-`    | 普通文件     | 最常见，存储数据 / 程序，无特殊功能              | 文本文件（.txt）、二进制程序（/usr/bin/ls）、图片 / 视频 / 压缩包 | 编辑、执行、复制、移动等常规操作      |                               |
| `d`    | 目录文件     | 用来存放其他文件 / 子目录，相当于「文件夹」      | /home、/etc、/tmp                                            | `cd` 进入、`mkdir` 创建、`rm -r` 删除 |                               |
| `l`    | 链接文件     | 类似 Windows 快捷方式，指向另一个文件 / 目录     | /bin -> /usr/bin、~/.bashrc（软链接）                        | 快速访问目标文件，不占额外空间        |                               |
| `c`    | 字符设备文件 | 按「字符流」读写数据（逐个字符处理），交互型设备 | /dev/tty1（终端）、/dev/zero（空字符设备）、打印机           | 终端输入输出、设备数据读写            |                               |
| `b`    | 块设备文件   | 按「块」读写数据（批量处理，效率高），存储设备   | /dev/sda（硬盘）、/dev/sdb1（U 盘分区）、/dev/sr0（光驱）    | 磁盘挂载、分区、读写文件系统          |                               |
| `s`    | 套接字文件   | 用于进程间通信（IPC），实现程序之间的数据交互    | /var/run/docker.sock（Docker 套接字）                        | 进程间通信（比如容器和宿主机通信）    |                               |
| `p`    | 管道文件     | 用于进程间传递数据（单向），临时缓存数据         | /tmp/xxx.pipe（自定义管道）                                  | 命令行管道（`                         | `）底层依赖，或自定义进程通信 |

三种查看方式：ls -l ， file ， stat

## 文件查找

### **find**

```shell
find [路径] [选项] [查找条件 + 处理动作]
```

1. 根据文件名查找 -name ， -iname （忽略大小写），“ens*”（文件名ens开头的）

2. 按文件大小 -size +5M，-size 5M，-size -5M
3. 指定查找的目录深度 -maxdepth ， -mindepth 
4. -a是同时满足，-o是或，not：条件取反
5. 按时间找 -mtime +5 ， -mtime 5 ， -mtime -5
6. 按照文件属主、属组找 -user（属主） ， -group（属组），-nouser（没有属主）， -nogroup（没有属组）
7. 按文件类型 -type f（f是普通文件） / d / l / b / c 
8. 按文件权限 -perm 644 ， -perm -644（权限最小是644，即大于等于644）
9. 按正则表达式 

```shell
[root@localhost ~]# find /etc -regex '.*ens[0-9][0-9][0-9].*'
# .*    任意多个字符
# [0-9]    任意一个数字
```

### 处理动作

-print 默认的处理动作

-ls 对查找到的文件执行 ls -l 命令

-delete 删除查找到的文件（-delete` 只能删文件 / 空目录，非空目录需用 `-exec rm -rf {} \;）

 -fls  /path/to/filename 将查找到的文件长格式信息保存到指定文件夹

```bash
find /etc -type d -perm 644 -fls /root/etc_dirs.txt
```

{}是占位符，代表「找到的每个文件的完整路径」

-exec 命令 {} \;

- 查找到root目录下面以.log结尾的文件，并且复制到/home/dir1中

```shell
find /root -name '*.log' -exec cp {} /home/dir1 \;
```

